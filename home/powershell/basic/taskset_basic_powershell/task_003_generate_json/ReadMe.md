# Generate Json

```bash
PS > $env:ENV_VAR1 = "env some value"
PS > $env:ENV_VAR2 = "env some value 2"
PS > $env:ENV_VAR3 = "env some value 3"
PS > ./generate_json.ps1               
Generating JSON with complex structure
.
PS > exit
$ tree                   
.
├── ReadMe.md
├── folder
│   └── test.json
└── generate_json.ps1
```
